import React from "react";
import Layout from "../layouts";
type Props = {};

const Home = function ({}: Props) {
    return (
        <Layout>
            
        </Layout>
    );
};

export default Home;
